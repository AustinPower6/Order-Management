# Haupt-Datenbank (Echtdaten)
auftragsabwicklung.db
auftragsabwicklung.db.*

# Echtdaten in app/daten (Kontenrahmen.db ist Referenz-DB, kein Echtdaten-DB)
app/daten/*.db
!app/daten/Kontenrahmen.db
app/daten/*.db.*
# Fallback-Protokoll (firmenspezifische Laufzeitdaten, gross geschrieben)
app/daten/ERROR.DB
app/daten/ERROR.DB*
# Token-Verbrauch-Protokoll (firmenspezifische Laufzeitdaten, gross geschrieben)
app/daten/TOKENS.DB
app/daten/TOKENS.DB*
# Verschlüsselte API-Keys/Secrets je Firma (DB v71) — dürfen nie auf GitHub
app/daten/api_keys_*.json
app/daten/backups/
app/daten/logos/
app/daten/artikel/
app/daten/*.log
app/daten/*.log.*
app/daten/*.zip
app/language.*.log.json
app/backups/

# Fehler-Logs lokal
ERROR.txt

# Lokale Einstellungen (Fenster-Geometrie, Theme)
app/settings.json

# Benutzerbezogene Einstellungen (personenbezogen, pro Benutzer)
app/settings_*.json

# Python-Bytecode
__pycache__/
*.pyc
*.pyo

# Generierte PDFs / Ausdrucke
Ausdrucke/
Belege/
Export/
Export.zip
app/Spool/

# Firmen-Vorlagen (geschäftsspezifisch)
Vorlagen/

# Versehentlich kopierte Temp-Dateien
UsersWalterAppDataLocalTemp*.json

# Lokales Startskript (enthält interne IP)
start-claude-local.bat

# DB-Export (Echtdaten)
app/DB-Export.json

# Lokale LM-Studio / vLLM Startskripte (enthalten interne IPs)
CLAUDE*.cmd
Claude Code.lnk

# Lokale Notizen
Netx.txt
Prompt.txt

# Entwickler-/Wartungsskripte (kein Teil der App)
tools/

# Firmen-Vorlagen und Logos (geschäftsspezifisch)
Vorlage Rechnungen neu.docx
Vorlage Rechnungen neu.jpg
app/Logo-001.png
app/Logo-002.png

# Test-/Hilfsskripte
test_druck.py
check_data.py

# Claude Code lokale Konfiguration
.claude/

# OS / Editor
Thumbs.db
.DS_Store
*.swp
.vscode/
.idea/
